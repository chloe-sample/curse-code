name: First Workflow
on: [push]

jobs:
  run-shell-commands:
    runs-on: windows-2019
    steps:
      - name: echo a string
        run: echo "Hello World"
      - name: Multiline Command
        run: |
          node -v
          npm -v

